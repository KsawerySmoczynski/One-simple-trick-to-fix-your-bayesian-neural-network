data:
  class_path: src.data.classification.MOON
  init_args:
    train_batch_size: 100
    test_batch_size: 100
    root: datasets
    train_ratio: 0.5
    validation_ratio: 0.5
    dataloader_args:
      num_workers: 4
      pin_memory: true
      persistent_workers: true
