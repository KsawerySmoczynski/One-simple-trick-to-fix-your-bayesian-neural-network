data:
  class_path: src.data.classification.MNIST
  init_args:
    train_batch_size: 512
    test_batch_size: 512
    root: datasets
    train_ratio: 0.7
    validation_ratio: 0.3
    dataloader_args:
      num_workers: 4
      pin_memory: true
      persistent_workers: true
