data:
  class_path: src.data.regression.UCI
  init_args:
    name: 'energy'
    train_batch_size: 32
    test_batch_size: 32
    train_ratio: 0.8
    validation_ratio: 0.2
    dataloader_args:
      num_workers: 4
      pin_memory: true
      persistent_workers: true
